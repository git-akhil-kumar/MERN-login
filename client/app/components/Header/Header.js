import React from 'react';

import { Link } from 'react-router-dom';

const Header = () => (
  <header>
    <nav>
     	<div>Header</div>
    </nav>

    <hr />
  </header>
);

export default Header;
